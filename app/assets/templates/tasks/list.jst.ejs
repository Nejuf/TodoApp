<ul>
	<% _(tasks).each(function(task){ %>
		<!-- <li class="task" data-id="<%= task.id %>">
			<%= task.title %>
		</li> -->
		<li class="task">
			<a href="#/tasks/<%= task.id %>">
				<%= task.title %>
			</a>
		</li>
	<% }) %>
</ul>